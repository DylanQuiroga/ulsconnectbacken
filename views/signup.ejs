<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Sign up</title>
</head>
<body>
  <h1>Registro</h1>
  <% if (error) { %>
    <p style="color:red"><%= error %></p>
  <% } %>
  <form method="POST" action="/signup">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <label>Correo universitario: <input name="correoUniversitario" required /></label><br />
    <label>Nombre completo: <input name="nombre" required /></label><br />
    <label>Contraseña: <input name="contrasena" type="password" required /></label><br />
    <button type="submit">Crear cuenta</button>
  </form>
  <p>¿Ya tienes cuenta? <a href="/login">Inicia sesión</a></p>
  <p><a href="/">Back to home</a></p>
</body>
</html>
